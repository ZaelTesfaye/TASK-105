[pytest]
# Run from repo root: python -m pytest unit_tests/ API_tests/
# Paths are relative to this file (repo/)
testpaths = unit_tests API_tests tests
addopts = --tb=short -q
filterwarnings =
    ignore::pytest.PytestDeprecationWarning
    ignore::DeprecationWarning
